# Capture Fixtures - Session A\n\nDate: 2025-11-28\nSession: A (Capture Layer)\nStatus: draft\nVersion: v1\nCapsule Type: capture-fixtures\n\nThis capsule documents sample CapturePayload fixtures and their expected Airtable `IntakeItems` mappings. Fixtures live under `fixtures/capture/`.\n\n---\n\n## 1. Email Inbound Fixture\n\n**File**: `fixtures/capture/email-inbound-sample.json`\n\n### 1.1 CapturePayload sample\n\nKey fields (see JSON file for full payload):\n\n- `source_type`: `email.inbound`\n- `source_system`: `email`\n- `source_id`: `email:MSG12345`\n- `artifact_type`: `email_message`\n- `job_id`: `22PKCUGRwQGg`\n- `job_name`: `ZZ / ZZT Fixture Job`\n- `author`: `client@example.com`\n- `created_at`: `2025-11-28T10:15:00Z`\n- `room`: `Kitchen`\n- `element`: `Cabinets`\n- `notes`: request to confirm cabinet layout and add a pull-out trash can\n- `capture_version`: `capture_payload_v1`\n\n### 1.2 Expected `IntakeItems` row\n\nWhen mapped to Airtable `IntakeItems`, this fixture should produce a row with:\n\n- `source_id` = `email:MSG12345`\n- `notes` = The `notes` field from the fixture\n- `author` = `client@example.com`\n- `job_id` = `22PKCUGRwQGg`\n- `job_name` = `ZZ / ZZT Fixture Job`\n- `artifact_type` = `email_message`\n- `created_at` = `2025-11-28T10:15:00Z`\n- `room` = `Kitchen`\n- `element` = `Cabinets`\n- `file_url` = null (blank)\n- `Attachments` = empty\n\nA future CI test can:\n- Load this JSON fixture,\n- Validate it against the CapturePayload v1 JSON schema,\n- Apply the CapturePayload→Airtable mapping,\n- Compare to an expected Airtable row representation.\n\n---\n\n## 2. Additional Fixtures (Future Work)\n\nFuture fixtures should cover other `source_type` values, for example:\n\n- `fireflies.meeting` - meeting transcript with summary and recording link.\n- `bubbles.note` - voice note transcription with tags.\n- `photo.ocr` - site photo with OCR text referencing a specific room/element.\n- `jt.daily_log` - JobTread daily log with multiple photos and notes.\n\nEach fixture should follow the same pattern: a CapturePayload JSON file under `fixtures/capture/` and a short documentation section in this capsule describing the expected Airtable row(s).\n